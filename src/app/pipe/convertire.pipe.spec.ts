import { ConvertirePipe } from './convertire.pipe';

describe('ConvertirePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertirePipe();
    expect(pipe).toBeTruthy();
  });
});
